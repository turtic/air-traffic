import React from 'react';
import logoGlobe from '../../assets/logoGlobe.svg';

const Header = (props) => (

    <header className="App-header">
        <img src={logoGlobe} className="App-logo" alt="logo" />
        <h1 className="App-title">AirApp</h1>

    </header>

);

export default Header;